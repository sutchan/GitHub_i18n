# GitHub i18n 版本更新历史

## 📝 版本更新记录

### v1.8.99 - 2024-06-02

**主要更新：核心功能修复与优化**

#### 关键 bug 修复

- 修复了`isNewerVersion`函数的参数错误，正确定义`newVersion`和`currentVersion`参数
- 优化了`hideNotification`方法，将参数从 DOM 元素改为通过 ID 查找元素，增强了代码健壮性
- 修复了本地存储操作中的潜在问题，确保配置项正确保存和读取
- 改进了定时器调用逻辑，避免可能的内存泄漏

#### 代码质量改进

- 更新了函数级注释，提高了代码可读性和维护性
- 统一使用`const`声明常量变量，遵循现代 JavaScript 最佳实践
- 优化了错误处理逻辑，增强了插件稳定性

#### 构建优化

- 修复了构建脚本中的语法错误，确保构建过程稳定可靠
- 完善了构建产物验证机制

### v1.8.62 - 2024-06-01

**主要更新：页面模式支持与性能优化**

#### 核心功能优化

- 实现了 `pageMode` 页面模式参数支持，使翻译引擎能够根据不同页面类型智能调整行为
- 优化了 `isImportantElement` 方法，添加了基于页面模式的特定判断逻辑
- 增强了 `shouldIgnoreElement` 方法，实现针对不同页面模式的定制化忽略规则
- 改进了 `isContentRelatedMutation` 方法，添加了页面模式特定的文本变化阈值
- 重构了 `detectImportantChanges` 方法，支持页面模式特定的性能配置和阈值

#### 性能优化

- 添加了基于页面模式的元素检测缓存机制，显著提高了重复检测的效率
- 实现了 `getTextChangeThreshold` 方法，为不同页面模式设置合理的文本变化阈值
- 完善了 `shouldSkipElementByPageMode` 方法，提供了更精细的元素过滤逻辑
- 针对 issues/pullRequests、wiki、search、codespaces 等特定页面模式优化了检测规则
- 优化了字符数据变化和子节点变化的处理逻辑，提高了翻译准确性

#### 代码质量改进

- 统一使用 `currentPageMode` 变量替代直接的 `pageMode` 参数，提高代码一致性
- 添加了页面模式存在性检查，增强了代码健壮性
- 优化了多处文本长度检查，使用页面模式特定的阈值配置
- 确保了所有核心方法都正确接收和使用页面模式参数

### v1.8.61 - 2024-05-25

**主要更新：基础优化**

- 修复了部分页面翻译不准确的问题
- 优化了 DOM 监听机制
- 完善了部分词典条目

## 📊 版本统计

| 版本    | 更新日期   | 主要内容               | 完成度  |
| ------- | ---------- | ---------------------- | ------- |
| v1.8.99 | 2024-06-02 | 核心功能修复与优化     | ✅ 100% |
| v1.8.62 | 2024-06-01 | 页面模式支持与性能优化 | ✅ 100% |
| v1.8.61 | 2024-05-25 | 基础优化               | ✅ 100% |

## 🔮 未来版本规划

### v1.9.0 - 计划中

- 完善翻译词典覆盖范围
- 进一步优化性能和用户体验
- 添加更多页面模式支持

### v2.0.0 - 计划中

- 添加用户自定义翻译选项
- 完善测试覆盖
- 优化大型词典的加载性能

## 版本信息

版本：1.0.0 - 最后更新：2024-06-02
